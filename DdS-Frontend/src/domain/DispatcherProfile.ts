export interface DispatcherProfile {
  nombre: string;
  correo: string;
  telefono: string;
  direccion: string;
  departamento: string;
  foto_url: string | null;
  paquetes_asignados: number;
}